Fixed:
  Team-enforced self-deleting messages are now correctly disabling the button to change it
  Fixed a bug where the receiver-side of self-deleting messages would fail to delete messages containing files, audios or images.
