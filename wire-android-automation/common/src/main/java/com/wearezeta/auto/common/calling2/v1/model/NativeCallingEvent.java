package com.wearezeta.auto.common.calling2.v1.model;

public enum NativeCallingEvent {
    // before we click a call button
    start,
    // incoming call overlay is visible
    incomingh,
    // accept button was clicked
    answer,
    // call went into active state
    dcestabh
}
